# 今後のタスク整理

最終整理: 2026-07-15

## 現状

- `npm run test`: 成功。8ファイル、22テスト通過。
- `npm run test:e2e`: 成功。ChromiumでP0追加分を含む6フロー通過。
- `npm run typecheck`: 成功。
- `npm run build`: 成功。
- `npm audit --omit=dev`: 脆弱性0件。
- 初期データ: 120項目、訳語案245件、使用例245件。
- Git状態: 基準コミット済み。P0の追加修正も全検証後のコミットで固定する。

## 対応済み

- 本番の弱い `SESSION_SECRET` を起動時に拒否するガードを追加。
- 停止済みユーザーをログイン中ユーザーとして扱わず、投稿・評価・コメント・通報導線を出さないように変更。
- README の初期管理者情報をローカル開発用として明記。
- 初回コミットで基準状態を固定。
- 検索で完全一致を優先し、タグ、分野、使用例、訳語案の文脈も検索対象に追加。
- 未登録語から新規項目作成へ進むとき、検索語をフォームへ引き継ぐように変更。
- 新規項目作成画面に近い項目を表示し、重複作成を減らす導線を追加。
- 訳語案カードから使用例だけを追加できるフォームを追加。
- 変更履歴をJSONの生表示から、項目別の変更前・変更後の差分表示へ変更。
- 管理者画面を追加し、ユーザー検索、ロール変更、停止、停止解除を画面から操作可能に変更。
- 編集者ダッシュボードに重複候補、評価割れ、推奨訳未整理、コメント増加のキューを追加。
- 推奨候補の並びをスコア優先に変更。
- 英字大文字・小文字の検索テストを追加。
- 通報対象を項目、使用例、コメントにも拡張。
- 編集者ダッシュボードで、項目、訳語案、使用例、コメントの通報対象リンクを表示。
- Server Actionのエラーをフォーム内に表示し、失敗時の入力値を復元する共通フォームを追加。
- 意味、訳語案、使用例の修正提案、編集者の直接編集、承認・却下キューを追加。
- 意味、訳語案、使用例の変更を履歴画面から理由付きで差し戻せるように変更。
- 表示名・ハンドル変更、本人によるパスワード変更、管理者の一時パスワード発行を追加。
- パスワード再設定時に既存セッションを失効させ、本人の変更完了まで投稿操作を止めるように変更。
- 利用規約とプライバシーポリシーの雛形文言を削除し、問い合わせ先、運営者名、投稿データ方針を環境変数化。
- 本番環境変数の起動前検査を追加。
- クリックジャッキング、MIME推測、過剰なReferer送信を抑えるHTTPセキュリティヘッダーを追加。
- seed再実行時の全データ削除を廃止し、非空DBでは安全にスキップするよう変更。
- PostgreSQLのseedでは既知のデモ用パスワードを作らず、強い初期管理者情報を必須化。
- ローカルPostgreSQLでmigration、seed、build、health check、backup、restoreを確認。
- Playwrightで検索、エラー表示、修正提案、履歴、パスワード再設定、プロフィール変更を確認。
- Playwrightで通報、訳語案の非表示、通報の処理済み・却下、ユーザー停止・解除を確認。
- 投稿・コメント・通報のレート制限を環境変数化し、運用初期値を決定。
- 月額上限0円の当面構成として、Vercel Hobby + Supabase Freeを選定。公開URLは無料の `*.vercel.app` を使う。
- ローカルPostgreSQLで環境検査、migration、seed、build、本番起動、health、backup、別DBへのrestoreを再確認。
- PC幅と390px幅で主要画面を目視し、管理者ログイン時のモバイルヘッダーのはみ出しを修正。
- 公開前チェックリスト、Staging一括検証、誤復元防止付き復元スクリプトを追加。
- 適用済みmigrationを書き換えず、公開準備のDB変更を追記型migrationへ分離。
- Server Actionで項目・意味・訳語案・使用例・コメントの親子関係を再検証し、改変されたIDの組み合わせを拒否。
- ログイン・登録にDB共有型のアカウント/IP別レート制限を追加。
- ユーザー停止は管理者だけに限定し、通報処理状態と推奨レベルをサーバー側で許可値に制限。
- 登録時に利用規約と投稿データ方針への明示同意を必須化し、同意日時・規約版・方針を保存。
- 新規PostgreSQLで2本のmigration、seed、PostgreSQL向けbuild、health check、backup、別DBへのrestoreを再確認。

## P0 公開前に必ず片付ける

### 1. 初回コミットと基準状態の確定

- 完了: 初回コミットを作成し、動作する状態を基準化する。
- 完了: `.env`、`prisma/dev.db`、`backups`、`.next`、`node_modules` をコミット対象外にする。
- 完了: README の初期管理者情報を開発用であると明記する。

完了条件:

- `git status` が意図した追跡状態になっている。
- 初回コミット後に `npm run test && npm run typecheck && npm run build` が通る。

### 2. 本番シークレットと認証の安全策

- 完了: `SESSION_SECRET` が本番で未設定、または `replace-with-a-long-random-secret` 相当の場合は起動に失敗させる。
- 初期管理者の `ADMIN_EMAIL`、`ADMIN_PASSWORD` を本番投入前に強い値へ変更する。
- 完了: 停止済みユーザーのセッション表示を見直し、投稿フォームを出さないようにする。
- 完了: ログイン・登録をアカウントとIPの両方でレート制限する。

完了条件:

- 本番環境で弱いセッション秘密鍵を使えない。
- 停止済みアカウントが投稿、評価、コメント、通報の導線を使えない。

### 3. PostgreSQL 本番経路の検証

- ステージング用PostgreSQLを用意する。
- 完了: ローカルPostgreSQLでmigration、seed、build、health checkを確認する。
- `npm run prisma:generate:postgres`、`npm run prisma:migrate:postgres`、`npm run build:postgres` を実DBで確認する。
- `npm run seed` を本番相当DBに流し、初期管理者と初期語彙を確認する。
- 完了: ローカルPostgreSQLで `npm run backup:postgres` と別DBへの復元を確認する。
- ステージングPostgreSQLでもバックアップと復元を一度試す。

完了条件:

- ステージング環境で検索、ログイン、投稿、評価、推奨訳設定、通報処理が動く。
- バックアップから復元できることを確認済み。

### 4. 法務・運用文書の正式化

- 完了: 利用規約とプライバシーポリシーの「公開前の雛形」文言を運用文書に置き換える。
- 完了: 投稿データは `site-only` に固定し、既存投稿へ遡ってCC条件へ切り替えない。
- 完了: 登録時に規約版と `site-only` 方針への同意を取得・記録する。
- 完了: 削除依頼、権利侵害申告、アカウント停止、問い合わせ先の扱いを明記する。
- 完了: 本番の問い合わせ先を `aidhide.support@gmail.com` に確定する。
- 完了: 公開する運営者名を `令和新漢語運営事務局` に確定し、責任者の氏名・住所は本人からの請求に応じて遅滞なく回答する方針を明記する。
- 完了: 当面は個人・非商用運営、投稿データは `site-only` とする。
- 保留: 専門家への法務確認は当面行わず、運用変更時に再検討する。

完了条件:

- `/legal/terms` と `/legal/privacy` が公開可能な内容になっている。
- 投稿ルール、規約、プライバシーポリシーの内容が矛盾していない。

### 5. 公開前セキュリティ確認

- `npm audit --omit=dev` を実行し、対応が必要な脆弱性を確認する。
- 完了: 投稿、コメント、通報のレート制限を環境変数化し、初期値を60秒につき12件、12件、8件に設定する。
- 通報から非表示、処理済み、却下までの編集者フローを実データで確認する。

完了条件:

- 既知の高リスク脆弱性が残っていない。
- 荒らし投稿を編集者が画面上で処理できる。

## P1 MVPの品質を上げる

### 1. 検索仕様を仕様書に近づける

- 完了: 完全一致を最上位に表示する。
- 完了: タグ、分野、使用例本文を検索対象に含める。
- 完了: 英字入力、大文字小文字、カタカナ表記揺れのテストを増やす。
- 完了: 未登録語から新規項目作成へ進むとき、検索語をフォームへ引き継ぐ。

完了条件:

- `SPEC.md` の FR-001 に対して、実装済み項目と未実装項目が明確になる。
- 検索ロジックの単体テストが増えている。

### 2. 投稿・編集フローの補強

- 完了: 使用例だけを追加できるフォームを作る。
- 完了: 既存の意味、訳語案、使用例を修正提案または直接編集できるようにする。
- 完了: 新規項目作成前に類似項目を表示し、重複を減らす。
- 完了: Server Action のエラーを画面内に表示し、失敗時に入力内容を復元する。

完了条件:

- 使用例追加、修正提案、重複確認が画面上で完結する。
- 失敗時もユーザーが次に何を直せばよいか分かる。

### 3. 変更履歴の実用化

- 完了: 履歴画面で `beforeJson` と `afterJson` の差分を表示する。
- 完了: 推奨訳と使用例の履歴も対象項目の履歴画面で追えるようにする。
- 議論要約のモデルと履歴を追加する。
- 完了: 編集者の差し戻し機能を追加し、理由入力を必須にする。

完了条件:

- 変更前後と理由が読める。
- 誤編集を編集者が画面から戻せる。

### 4. 編集者ダッシュボードの拡張

- 完了: 重複候補、評価が割れている訳語、古いままの項目、コメントが増えた議論をキュー化する。
- 完了: 推奨候補を単なる更新順ではなく、評価内訳、使用例有無、スコアで並べる。
- 完了: 通報対象を訳語案だけでなく、項目、使用例、コメントにも広げる。

完了条件:

- 編集者が「次に整理すべきもの」をダッシュボードから判断できる。
- 通報対象ごとに適切な処理画面へ移動できる。

### 5. アカウント管理

- 完了: 管理者向けにロール変更、停止解除、ユーザー検索を用意する。
- 完了: 管理者による一時パスワード発行と、次回ログイン時の変更要求を追加する。
- 完了: 表示名、ハンドルの変更導線を作る。

完了条件:

- 管理者が最低限のアカウント運用を画面上で行える。
- パスワードを忘れたユーザーへの対応手順がある。

## P2 公開後の成長タスク

### 1. 評判値と信頼ユーザー

- 投稿、評価、採用、通報処理の結果から `reputation` を更新する。
- 信頼ユーザーにタグ整理や軽微な修正を許可する。
- 権限昇格と降格の基準を運用ルールにする。

### 2. 検索基盤の強化

- PostgreSQL full text search と `pg_trgm` を導入する。
- 表記揺れ辞書を増やす。
- 検索結果で「なぜ一致したか」を表示する。

### 3. 通知と再訪導線

- 自分の投稿へのコメント、推奨訳採用、通報結果を通知する。
- 編集者向けに未処理キューの増加を通知する。

### 4. データ公開と外部連携

- 訳語データのエクスポート形式を決める。
- API、ブラウザ拡張、文章一括変換は公開後の利用状況を見て判断する。

### 5. テストと品質保証

- 完了: Playwright で主要フローのE2Eテストを追加する。
- 完了: PC幅と390px幅で主要画面、フォーム、ダッシュボードを目視確認する。公開後も定期確認する。
- seed データの品質レビュー手順を作る。

## 条件分岐つき実行計画

### A. 公開判断

条件: 1週間以内に外部公開する。

- Yes: P0以外の機能追加は止める。法務文書、本番DB、環境変数、バックアップ、ヘルスチェック、運用手順だけを進める。
- No: 公開ブロッカーを残したまま、こちらだけで進められるP1実装を先に片付ける。

止める条件:

- `/legal/terms` または `/legal/privacy` に「公開前の雛形」が残っている。
- 問い合わせ先、削除依頼、権利侵害申告の受け口が決まっていない。
- ステージングPostgreSQLで、ログイン、投稿、評価、コメント、通報、推奨訳設定、非表示、停止解除を一巡できていない。
- `SESSION_SECRET`、`ADMIN_EMAIL`、`ADMIN_PASSWORD` が本番用の強い値になっていない。
- バックアップと復元を一度も確認していない。

公開してよい条件:

- 法務文書が公開用に確定している。
- ステージングで主要操作が通っている。
- `npm run test`、`npm run typecheck`、`npm run build`、`npm audit --omit=dev` が通っている。
- 初期管理者の開発用認証情報を本番に使っていない。

### B. 法務文書とライセンス

条件: 投稿データを将来APIやデータセットとして外部再利用させたい。

- Yes: 投稿データのライセンスを CC BY か CC BY-SA に決め、利用規約へ明記する。投稿者が何を許諾するかを登録前に読める状態にする。
- No: 当面は「サイト内での掲載、編集、要約、検索表示」だけを許諾範囲として書く。外部配布は後日、別途明示する。

条件: 問い合わせ先を用意できる。

- Yes: 利用規約、プライバシーポリシー、投稿ルールに同じ問い合わせ先を記載する。
- No: 公開を延期する。削除依頼と権利侵害申告の受け口なしでは運用しない。

条件: 実名や企業名を含む投稿を許容する。

- Yes: 個人情報、社外秘、名誉毀損、引用範囲の扱いを規約と投稿ルールに明記する。
- No: 投稿ルールで実在個人、社外秘、長文引用、企業内部情報を禁止し、通報理由にも反映する。

完了条件:

- 「公開前の雛形」文言が消えている。
- 投稿データの利用範囲が明確。
- 禁止事項、通報、非表示、アカウント停止、問い合わせ先が矛盾なく書かれている。

### C. PostgreSQLステージング

条件: 使用するPostgreSQLサービスが決まっている。

- Yes: `DATABASE_URL` と `DIRECT_URL` を設定し、`npm run prisma:migrate:postgres`、`npm run seed`、`npm run build:postgres` をステージングで実行する。
- No: Supabase、Neon、Render PostgreSQL などから1つ選び、先にステージングDBを作る。比較に時間を使いすぎない。

分岐:

- migration が失敗する: `scripts/write-postgres-schema.mjs`、生成された `prisma/schema.postgres.prisma`、migration SQL を確認する。
- seed が失敗する: `ADMIN_EMAIL`、`ADMIN_PASSWORD`、重複slug、必須データ不足を確認する。
- build は通るが実操作が失敗する: pooler設定、serverless接続、`DIRECT_URL` と `DATABASE_URL` の使い分けを確認する。
- `/api/health` が失敗する: DB接続文字列、マイグレーション適用状況、実行環境の環境変数を確認する。
- バックアップが失敗する: `pg_dump` の接続先、SSL設定、権限、IPv4/IPv6制約を確認する。

完了条件:

- ステージングで `/api/health` が成功。
- 初期管理者でログインできる。
- 項目作成、意味追加、訳語案追加、使用例追加、評価、コメント、通報、推奨訳設定、通報処理、ユーザー停止/解除が通る。
- `npm run backup:postgres` の出力から復元できる。

### D. こちらだけで進められる実装

条件: 法務文書または本番DBがまだ決まらない。

- Yes: 以下を上から順に実装する。
- No: 公開準備を優先し、実装追加は公開後に回す。

実装順:

1. 完了: Server Action のエラーを画面内に表示する。
2. 完了: 投稿失敗時に入力値を復元する。
3. 完了: 既存の意味、訳語案、使用例の修正提案を作る。
4. 完了: 編集者による直接編集と変更履歴保存を作る。
5. 完了: 編集者の差し戻し機能を作る。
6. 完了: 表示名、ハンドル変更を作る。
7. 完了: 管理者による一時パスワード発行を作る。
8. 完了: Playwrightで主要フローのE2Eテストを作る。

分岐:

- Server Action のエラー表示を先にやる場合: フォーム単位の状態管理が必要。ログイン、投稿、評価、通報から順に対応する。
- 修正提案を先にやる場合: `Revision` だけでは足りないため、提案状態を持つ新モデルを検討する。
- 直接編集を先にやる場合: 編集者権限に限定し、必ず `beforeJson`、`afterJson`、理由を保存する。
- パスワード再設定を先にやる場合: メール送信なしのMVPでは、管理者による一時パスワード発行に限定する。

止める条件:

- 編集や差し戻しで履歴が残らない設計になる。
- 一般ユーザーが編集者向け操作を実行できる。
- エラー表示のために既存フォームの入力体験が大きく悪化する。

### E. 公開後の観測と分岐

条件: 検索利用は多いが投稿が少ない。

- 未登録語検索結果に「この語を投稿」だけでなく「既存項目に訳語案を足す」を出す。
- 投稿フォームの必須項目を見直す。
- トップページに未整理語、草案、使用例不足の語を出す。

条件: 投稿はあるが品質がばらつく。

- 投稿フォームに文脈、弱点、使用例の入力補助を追加する。
- 草案と正式候補の表示差を強める。
- 編集者キューで使用例なし、理由なし、評価割れを優先する。

条件: 荒れやすい。

- 新規ユーザーの投稿、コメント、通報のレート制限を強める。
- 通報理由の分類を増やす。
- コメント非表示、使用例非表示、項目凍結を追加する。

条件: 編集者作業が詰まる。

- 推奨候補の自動並び替えを改善する。
- コメント増加、評価割れ、古い未整理項目をダッシュボード上部に出す。
- 議論要約と差し戻しを先に実装する。

条件: 検索精度が不足する。

- PostgreSQL full text search と `pg_trgm` を導入する。
- 表記揺れ辞書を増やす。
- 検索結果に「一致理由」を表示する。

### F. 優先順位の決め方

条件: 公開日が決まっている。

- P0だけを進める。P1/P2は凍結する。

条件: 公開日は未定だがデモを見せたい。

- エラー表示、修正提案、プロフィール変更を優先する。外部DBや法務文書はデモ範囲外として明記する。

条件: 実ユーザー投稿を受ける。

- 法務文書、通報運用、バックアップ、停止解除、レート制限を優先する。

条件: 身内だけで試す。

- UI改善、編集フロー、seed品質レビューを優先する。

## 次の1スプリント推奨順

1. Supabase Freeで本番とは別のステージングDBを用意し、migration、seed、build、主要操作を一巡する。
2. ステージングの `pg_dump` と接続先PostgreSQLのメジャーバージョンを合わせ、backup、restoreを確認する。
3. Vercel HobbyのHTTPS環境で `npm run check:production-env` と `/api/health` を確認する。
4. 本番用の強い `SESSION_SECRET`、管理者メール、管理者パスワードをVercelへ登録する。
5. 公開後1週間のログ確認と、無料枠・手動バックアップ運用の見直し日を決める。
